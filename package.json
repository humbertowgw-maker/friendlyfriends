{
  "name": "friendlyfriends",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.19.0"
  },
  "description": "Zero-budget animated pet-episode pipeline (Pollinations image gen, Edge TTS, FFmpeg) plus an AI provider cost/rate-limit dashboard and the Maria CRDT-synced chat pet",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run yjs\" \"npm run client\"",
    "server": "node server/index.js",
    "yjs": "node server/yjs-server.js",
    "client": "cd client && npm run dev",
    "setup": "npm install && cd client && npm install",
    "build": "cd client && npm run build",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "chokidar": "^5.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "gray-matter": "^4.0.3",
    "sql.js": "^1.12.0",
    "y-websocket": "^2.1.0"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "vitest": "^4.1.10"
  }
}
